using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Points : MonoBehaviour
{
    // Start is called before the first frame update
    /*void Awake(){
       CapsuleCollider2D cap = GetComponent<apsuleCollider2D>;
       Vector2[] points = cap.points;
       EdgeCollider2D Edege = gameObject.AddComponent<EdgeCollider2D>
       Edege.points= points;
       Destroy(cap);
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
